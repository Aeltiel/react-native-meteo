import { StyleSheet } from "react-native";

const sBStyle = StyleSheet.create({
  input: {
    backgroundColor: "white",
    height: 40,
    paddingLeft: 20,
    borderRadius: 20,
    fontFamily: "Alata-Regular",
    elevation: 5,
  },
});

export default sBStyle;
